tasks:
  - name: e2e
    description: Run the full end-to-end test suite
    actions:
      - cmd: |
          echo "TODO: Implement tests"
  - name: package-up
    description: Build and deploy a test package that contains a workflow template and a test pod
